## 多线程

3种方式
1. extends Thread
2. implements Runnable
3. implements Callable<T>


第一种和第二种本质上一样，Thread 类也是实现了 Runnable 接口，一个没有返回值的 run 方法

```java
@FunctionalInterface
public interface Runnable {
    public abstract void run();
}
```

继承Thread
```java
public class C extends Thread{
    @Override
    public void run() {
        // TODO
    }
    public static void main(String[] args) {
        new C().start();
    }
}
```

实现 Runnable 接口
```java
public class C implements Runnable{
    @Override
    public void run() {
        // TODO
    }
    public static void main(String[] args) {
        new C().run();
    }
}
```


实现 Callable 接口
```java
//定义返回值泛型，这里我设置为String
public class C implements Callable<String> {

    // call() 方法会抛异常，还会使用Callable定义的返回值类型
    @Override
    public String call() throws Exception {
        return null;
    }

    public static void main(String[] args) throws Exception {
        String call = new C().call();
    }  
}
```


### 线程优先级

一个10个优先级，主线程优先级为5