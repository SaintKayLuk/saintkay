
## 函数式接口


函数式接口可以使用 Lambda 表达式来创建实例


### 特性和限制

1. 只包含一个抽象方法，当然接口里的普通方法默认都是抽象的，可以省略 abstract
2. 可以包含 **默认方法** 和 **静态方法**
3. 用 @FunctionalInterface 注解标记，可选，此注解会检查接口是否属于函数式接口，例如有1个以上抽象方法，就会报错



java自带的函数式接口，例如 Runnable，Callable，Function 等，包括 java.util.function 包下的所有接口