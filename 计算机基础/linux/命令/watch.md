查看命令的输出

```
watch [参数] 命令
    -n, --interval <secs>   间隔几秒，例如可以 -n 0.5 代表每0.5秒刷新一次
    -d                      高亮显示变化的数值
    -t,--no-title           隐藏头部，默认最顶部显示间隔时间和查看的命令
```

例：查看英伟达显卡的使用率，每2秒查看一次，相当于每2秒执行一次 nvdia-smi
```sh
#查看英伟达显卡使用率命令为 nvdia-smi 但是只是一次输出

watch -n 2 -d nvdia-smi
```