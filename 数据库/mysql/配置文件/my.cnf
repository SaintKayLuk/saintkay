[mysqld]
wait_timeout 



innodb_flush_log_at_trx_commit = 1

1 默认 可选 0,1,2


控制提交操作严格遵循 ACID和重新排列并批量完成提交相关 I/O 操作时可能实现的更高性能 之间的平衡 。您可以通过更改默认值来实现更好的性能，但这样一来，您可能会在崩溃时丢失事务。

完全符合 ACID 要求，默认设置 1。每次提交事务时，日志都会写入并刷新到磁盘。

如果设置为 0，则每秒将日志写入并刷新到磁盘一次。未刷新日志的事务可能会在崩溃时丢失。

如果设置为 2，则每次事务提交后都会写入日志，并且每秒刷新一次到磁盘。未刷新日志的事务可能会在崩溃时丢失。

对于设置 0 和 2，每秒刷新一次并不能 100% 保证。由于 DDL 更改和其他内部InnoDB 活动导致日志独立于 innodb_flush_log_at_trx_commit 设置刷新，刷新可能会更频繁地发生，有时由于调度问题刷新频率会更低。如果每秒刷新一次日志，则崩溃时最多可能会丢失一秒钟的事务。如果刷新日志的频率高于或低于每秒刷新一次，则可能丢失的事务数量会相应变化。

日志刷新频率由 控制 innodb_flush_log_at_timeout，它允许您将日志刷新频率设置为 N秒（其中 N为1 ... 2700，默认值为 1）。但是，任何意外的mysqld进程退出都可能擦除最多N秒的事务。

DDL 更改和其他内部InnoDB 活动独立于设置刷新日志 innodb_flush_log_at_trx_commit 。

InnoDB 无论设置如何， 崩溃恢复innodb_flush_log_at_trx_commit都可以工作 。事务要么全部应用，要么全部删除。

为了在使用事务的复制设置中实现持久性和一致性InnoDB：

如果启用了二进制日志记录，请设置 sync_binlog=1。

始终设置 innodb_flush_log_at_trx_commit=1。